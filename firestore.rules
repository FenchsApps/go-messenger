
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешить полный доступ ко всем документам
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
