rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to all users.
    // This is not recommended for production apps.
    // You should secure your data based on your app's requirements.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
