rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Разрешить чтение и запись, если пользователь аутентифицирован
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
